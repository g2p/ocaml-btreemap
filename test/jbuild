(jbuild_version 1)

(executables
    ((libraries (btreemap))
     (names (test))
     (modules (test))))

(alias
    ((name runtest)
     (package btreemap)
     (deps (test.exe))
     (action (run ${<}))))
